{
  "name": "sppucodes-db",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker for SPPU Codes analytics — D1 database operations",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "db:create": "wrangler d1 create sppucodes-db",
    "db:execute": "wrangler d1 execute sppucodes-db --file=schema.sql",
    "db:migrate": "wrangler d1 execute sppucodes-db --command=\"SELECT name FROM sqlite_master WHERE type='table'\"",
    "secret:set": "wrangler secret put DB_API_KEY"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241201.0",
    "typescript": "^5.7.0",
    "wrangler": "^3.91.0"
  }
}
